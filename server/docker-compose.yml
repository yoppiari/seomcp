version: '3.8'

services:
  mcp-seo-server:
    build: .
    container_name: mcp-seo-server
    restart: unless-stopped
    ports:
      - "4001:4000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=4000
      - SESSION_SECRET=${SESSION_SECRET:-change-this-secret-in-production}
      - API_KEY=${API_KEY:-2f3bd395c3a85ec7e2b704b57f07dfac0a6d624c13c536479ca5809db202bcd0}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mcp-seo-new.rule=Host(`seo.modalhp.com`)"
      - "traefik.http.routers.mcp-seo-new.entrypoints=websecure"
      - "traefik.http.routers.mcp-seo-new.tls=false"
      - "traefik.http.services.mcp-seo-new.loadbalancer.server.port=4000"
    networks:
      - web

networks:
  web:
    external: true
